# React Bootstrap chunk files and other node clutter
# Ignore React build files and other chunks generated during bundling
Frontend/node_modules/.vite/deps/react-router-dom.js
Frontend/node_modules/.vite/deps/react-dom_client.js.map
*.js

# Node modules directory
/node_modules/

# Build output directories (these are generated during the build process)
# Ignore output from build tools like Webpack, Parcel, or Vite
/build/
/dist/

# Log files
*.log

# OS generated files (These are created by macOS, Windows, etc.)
.DS_Store   # macOS
Thumbs.db   # Windows

# npm package lock files (optional)
# package-lock.json is often used for locking versions in a Node.js environment,
# but it’s optional depending on your project setup.
# Uncomment the next line if you don't want to commit package-lock.json
# package-lock.json

# Yarn lock file (if using Yarn package manager)
yarn.lock

# Coverage directory from testing (e.g., generated by Jest or other testing frameworks)
/coverage/

# IDE / editor folders (can vary depending on the editor you're using)
.vscode/
.idea/

# Environment variable files (can contain secrets, API keys, etc.)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# React Native related files (if using React Native)
android/
ios/

# Temporary files created by build tools
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Parcel-bundler cache (if using Parcel)
.cache/

# Next.js build artifacts (if using Next.js with React)
.next/

# Expo / React Native project files
.expo/
.expo-shared/
