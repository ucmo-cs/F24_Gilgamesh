# React Bootstrap chunk files and other node clutter
Frontend/node_modules/.vite/deps/react-router-dom.js
Frontend/node_modules/.vite/deps/react-dom_client.js.map
*.js

# Node modules directory
/node_modules/

# Build output
/build/
/dist/

# Log files
*.log

# OS generated files
.DS_Store  # macOS
Thumbs.db  # Windows

# npm package lock files (optional, depends on whether you want to commit these)
# package-lock.json is often used for locking versions in a Node.js environment, 
# but if you're working in teams, it's a good idea to include it.
# package-lock.json is often committed in React projects, but it can be excluded if desired.

# Uncomment the next line if you don't want to commit package-lock.json
# package-lock.json

# Yarn lock file (if using Yarn package manager)
yarn.lock

# Coverage directory from testing
/coverage/

# IDE / editor folders (can vary depending on the editor)
.vscode/
.idea/

# Environment variable files (contains secrets, API keys, etc.)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# React Native related files (if you're using React Native)
android/
ios/

# Temporary files created by build tools
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Parcel-bundler cache
.cache/

# Next.js build artifacts (if using Next.js with React)
.next/

# Expo / React Native project files
.expo/
.expo-shared/
